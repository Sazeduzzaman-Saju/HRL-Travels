import React from 'react';
import './PrimaryButton.css'

const PrimaryButton = ({ children }) => {
    return (
        <button className='button-86'>{children}</button>
    );
};

export default PrimaryButton;